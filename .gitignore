node_modules/
package-lock.json

# WhatsApp session — never commit, contains live login credentials
auth_info/

# Environment secrets
.env

# Runtime data generated by the bot — regenerated on first run
settings.json
words.json
games.json
names.json
lidcache.json
customWords.json

# Logs
*.log
npm-debug.log*